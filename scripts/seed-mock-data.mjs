#!/usr/bin/env node
/**
 * Seed mínimo multi-tenant (stub).
 * Requiere CORE_TABLE_NAME + credenciales AWS locales.
 *
 * Uso: npm run seed:mock
 */
console.log(
  JSON.stringify({
    level: 'INFO',
    message: 'seed-mock-data stub — cablear PutItem TENANT#/MEMBER#/ACCOUNT# en fases siguientes',
  }),
);
